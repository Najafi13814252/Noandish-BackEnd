courses
    id
    image_url
    title
    lessons
    members
    duration
    teacher_id (FK => teachers.id)
    points
    price
    discount
    level
    prerequisite
    language
    created_at

teachers
    id
    fullname    
    avatar
    bio
    created_at

course chapters   
    id
    course_id (FK => course.id)
    title

lessons
    id
    chapter_id (FK => chapters.id)
    duration
    video_url
    is_free

comments
    id
    course_id (FK => courses.id)
    user_id (FK => users.id)
    comment
    rating
    created_at